/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keeps the build from failing on style warnings while you are learning.
  eslint: { ignoreDuringBuilds: true },
  poweredByHeader: false,
};

export default nextConfig;
